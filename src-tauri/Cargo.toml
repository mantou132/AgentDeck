[package]
name = "agentdeck"
version = "0.1.0"
description = "A focused mobile ACP client built with Tauri"
authors = ["AgentDeck contributors"]
edition = "2024"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "agentdeck_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-edge-to-edge = { git = "https://github.com/mantou132/tauri-plugin-edge-to-edge", rev = "86fe04372c33d1fc0a2459d3c49ee1b5be1324d9" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
